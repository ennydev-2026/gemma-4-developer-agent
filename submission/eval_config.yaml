# Stage 1 per-task limits. Approximately 120 hidden tasks share a 12-hour
# inference budget, so uniform long timeouts are unsafe. Re-tune these values
# only from measured target-model traces.
evaluation:
  timeout_seconds: 120
  max_tool_calls: 40
  max_time_minutes: 6
  max_turns: 80
